# 2025.12.30 - JRN - Table Bug Hunt

## Context
- User reported getting stuck on a long scan: "Examining markdown_renderer.rs for table issues".
- Continue table bug hunting and fixes based on regression images.

## Observations
- `render_table_tablebuilder` uses `ColumnSpec::as_column()` in the hscroll branch; resolved widths and content width may diverge from actual layout, leading to edge clipping or unstable widths.
- `content_width` is derived from `resolved_widths`, not from the column layout.

## Actions
- Created this journal.
- Re-opened table rendering code to verify width/scroll behavior.

## Next
- Inspect `apply_persisted_widths`, `handle_width_change`, and column width persistence to confirm stable IDs and width persistence under hscroll.
- Reconcile resolved widths vs. column layout when hscroll is enabled.
