# Journal - Image Cache LOD Implementation

## 2026-03-11

### Plan

Implement the LOD (Level-of-Detail) image cache redesign per the HLD at
`wrk_docs/2026.03.11 - HLD - Image Cache LOD.md`.

**Stages:**

1. **Stage 1 — ImageCacheEntry + ImageCache structural changes**
   - Add `degraded: bool` to `ImageCacheEntry`
   - Add `degrade()` method to `ImageCache`
   - Add `enforce_budget()` method to `ImageCache`
   - Remove eviction loop from `insert()` (keep entry-count safety valve)
   - Remove `pick_eviction_candidate()`
   - Remove `touch()` from `get()` and delete `touch()`
   - Write tests for degrade, enforce_budget, insert without eviction
   - **Commit**

2. **Stage 2 — Rendering path: degraded reload logic**
   - Modify `get_or_load_image_texture` to detect degraded entries
   - Add budget check before enqueuing reload of degraded entry
   - Use previous-frame viewport distances for reload-zone decision
   - Handle degraded+stale entries (don't remove, enqueue reload)
   - Write tests
   - **Commit**

3. **Stage 3 — Frame lifecycle: call enforce_budget**
   - Call `enforce_budget()` at end of `render_to_ui()` after viewport
     distances are computed
   - Pass `egui::Context` through to the cache
   - Wire up the full frame sequence: poll → render → collect → distances → enforce
   - Write integration tests
   - **Commit**

4. **Stage 4 — Cleanup + final review**
   - Remove dead code (pick_eviction_candidate, old eviction tests)
   - Update any status bar / tooltip code if needed
   - Full build + test pass
   - Code review + devils advocate
   - **Commit**

### Log

- **T+0**: Starting Stage 1 implementation.
- **T+26min**: Stage 1 complete. All structural changes done:
  - `degraded: bool` added to `ImageCacheEntry`
  - `degrade()`, `enforce_budget()`, `would_restore_fit()` added
  - `insert()` simplified (no byte eviction, count-only safety valve)
  - `get()` now `&self`, `touch()` removed
  - `pick_eviction_candidate()` removed
  - 8 new tests, 11 cache tests total pass
  - Debug + release build clean, zero warnings
- **T+26min**: Devils advocate reviewed HLD vs code. Key findings:
  - Findings #5/#6 (no reload logic for degraded images, no budget check) → Stage 2 work
  - Finding #8 (staleness on degraded entries) → Stage 2 work
  - Finding #10 (enforce_budget not called) → Stage 3 work
  - Finding #2 (data URI key mismatch) → pre-existing issue, not LOD-related
  - Finding #3 (RefCell latent risk) → acceptable, enforce_budget runs outside render
  - All other findings LOW/documented
- **T+26min**: Code review of Stage 1 in progress.
- **T+50min**: Stage 2 complete. `get_or_load_image_texture` handles 4 cases:
  cached/not-degraded/not-stale, degraded/not-stale (reload if in zone + budget),
  degraded/stale (reload unconditionally), not-degraded/stale (remove as before).
  Added `last_viewport_height` tracking, `enqueue_degraded_reload` helper. 2 new tests.
- **T+60min**: Stage 3 complete. `enforce_budget()` wired into `render_to_ui()`
  after `update_viewport_distances()`. Removed dead `#[allow(dead_code)]`. 3-line change.
- **T+75min**: Stage 4 complete. Cleanup: removed stale `#[allow(dead_code)]` from
  `degrade()`, `would_restore_fit()`, `LruCache::clear()`. Clippy clean. All tests pass.
- **T+80min**: Final verification: 30/30 HLD checklist items PASS.
- **T+80min**: Devils advocate final review found 5 issues:
  - #1 (degrade→restore race): NOT A BUG — arithmetic is correct
  - #2 (empty distances frame 1): By design, acceptable one-frame delay
  - #3 (stale entries from old docs): VALID — orphan entries without distances
    can't be degraded. Fixed by adding Phase 1 to enforce_budget that degrades
    orphans BEFORE current-document entries. Test caught a real ordering bug
    where the only current-doc entry was degraded before orphans.
  - #4 (texture handle leak): NOT A BUG — egui Arc-based Drop cleanup
  - #5 (saturating underflow): Acceptable defensive coding
- **T+90min**: All stages complete. 5 commits total.
- **T+100min**: Applied review feedback: saturating_sub in would_restore_fit,
  8 stale "eviction" doc comments updated, test comment cleaned up.
- **T+100min**: Background review agents completed. Triaged findings:
  - Stage 1 reviewer: orphan entries (fixed), saturating_sub (fixed), docs (fixed)
  - Stage 2 reviewer: no bugs found, minor test strengthening suggestions
  - Stage 2 devils advocate: misunderstood egui ScrollArea rendering (all
    children rendered every frame, even off-screen). Data URI degradation
    noted as known limitation (trivial edge case — data URIs are small).

### Final state
- 7 commits: 4 feature stages + orphan fix + doc updates + review fixes
- All tests pass (image_cache, degrade, enforce_budget, get_or_load, would_restore_fit)
- Debug + release build clean, zero warnings, clippy clean
- HLD fully implemented (30/30 checklist items verified)
- Known limitation: data URI images can't restore after degradation (acceptable —
  data URIs in markdown are typically small and won't trigger degradation)
