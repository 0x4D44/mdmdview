# Journal - Pending Restore Budget Implementation

## 2026-03-11

### Plan

Implement the pending restore budget fix per the HLD at
`wrk_docs/2026.03.11 - HLD - Pending Restore Budget.md`.

This is a small, focused fix: one new field, three touch-points, one
updated method.

**Stages:**

1. **Stage 1 — ImageCache field + updated would_restore_fit + tests**
   - Add `pending_restore_bytes: usize` to `ImageCache`
   - Initialise to 0 in `new()`, clear in `clear()`
   - Update `would_restore_fit` to include `pending_restore_bytes`
   - Write tests for the updated budget check
   - **Commit**

2. **Stage 2 — Increment on enqueue + decrement on result + tests**
   - Increment `pending_restore_bytes` after successful degraded reload enqueue
   - Decrement in `poll_image_results` for current-gen degraded results
   - Zero in `reset_image_state`
   - Do NOT decrement for stale-generation discards
   - Write tests
   - **Commit**

3. **Stage 3 — Review + devils advocate + cleanup**
   - Code review
   - Devils advocate
   - Apply feedback
   - Full build + test
   - **Commit if needed**

### Log

- **T+0**: Starting implementation.
- **T+15min**: Stages 1+2 complete (implemented as single change):
  - `pending_restore_bytes: usize` added to `ImageCache`, init to 0, cleared in `clear()`
  - `would_restore_fit` updated: `+ self.pending_restore_bytes` in budget formula
  - `enqueue_degraded_reload` return type changed to `bool`
  - Increment after successful enqueue in `get_or_load_image_texture` (Cases 2+3)
  - Decrement in `poll_image_results` for current-gen degraded results only
  - Zeroed in `reset_image_state`
  - 4 new tests: would_restore_fit with pending, clear, reset, init
  - All tests pass (11 cache, 8 degrade, 4 enforce, 3 pending, 2 would_restore)
  - Debug + release build clean, zero warnings
- **T+20min**: Code review — no bugs found. RefCell borrow safety verified.
  Slightly conservative double-counting in would_restore_fit noted as correct
  direction to err.
- **T+20min**: Devils advocate — no critical bugs found. Findings:
  - Double-decrement: SAFE (image_pending + degraded flag prevent)
  - Increment without decrement (worker crash): MEDIUM, bounded by doc lifetime
  - Case 3 stale enqueue: CORRECT (increments only on successful enqueue)
  - Estimate vs actual mismatch: BY DESIGN (documented in HLD)
  - Zero-size entries: SAFE (benign)
- **T+25min**: No feedback to apply. Committed.
- **T+30min**: Second round of reviews (re-ran after context compaction):
  - Code review #2: Approve. Full HLD conformance table (13 findings, all OK/NIT).
    Suggested test for decrement path and debug_assert before saturating_sub.
  - Devils advocate #2: No critical bugs. 9 findings:
    - #3 (MEDIUM): Worker panic → permanent counter leak. Suggested self-correction.
    - #4 (MEDIUM): `remove()` / capacity eviction doesn't adjust counter. Noted as
      latent — current call sites safe (Case 4 removes non-degraded entries only).
    - #8 (LOW): `saturating_sub` masks bugs → suggested debug_assert.
    - #9 (MEDIUM): No self-correction mechanism within document session.
  - Applied two defensive improvements:
    1. `debug_assert!` before `saturating_sub` to catch drift in dev builds.
    2. Self-correction in `poll_image_results`: if `image_pending` empty but
       `pending_restore_bytes > 0`, reset to 0 (handles worker panics, drift).
  - Committed as `chore: add defensive guards to pending_restore_bytes counter`.
- **T+35min**: Final verification agent: 46/46 PASS across both HLDs (30 LOD + 16 pending).

### Final state
- 2 commits: feature implementation + defensive guards
- All tests pass (11 cache, 8 degrade, 4 enforce, 3 pending, 2 would_restore)
- Debug + release build clean, zero warnings, clippy clean
- Both HLDs fully implemented and verified (46/46 checklist items)
- Known limitation: single-image oscillation (pre-existing, not caused by this change)
