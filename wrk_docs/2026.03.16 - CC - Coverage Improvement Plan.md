# Coverage Improvement Plan — Target 98%

**Date:** 2026-03-16
**Baseline:** 93.56% lines (846 missed of 13,142)
**Target:** 98.0% lines (max 263 missed)
**Lines to cover:** 583

---

## Strategy

Production code is at 99.71% — the gap is almost entirely in `bin/full_test.rs` (813 missed lines, 48.71%). The plan focuses primarily on improving full_test.rs coverage through testable utility functions, visual test logic, and HTML report generation paths.

---

## Stage 1: Production Code Quick Wins (~15 lines)

**Target:** Cover easily-testable production code gaps.

### 1a. `markdown_renderer.rs` — table column layout edge cases
- Add tests for `build_table_column_layout` rare column policy paths (lines 4580, 4589, 4597, 4600, 4608)
- These are the `clip(true)` paths and `Remainder`/`auto_with_initial_suggestion` for scaled-down scenarios

### 1b. `markdown_renderer.rs` — mermaid theme menu and image popup
- Test the mermaid theme menu rendering path (line 2059)
- Test image info popup rendering (lines 3229-3234)

### 1c. `diagram_common.rs` — minor branch coverage
- Exercise the 2 missed lines/branches in render_block error paths

### 1d. `window_state.rs` — error path coverage
- Test registry/file I/O error handling (2 missed lines)

**Expected improvement:** ~15 production lines covered → overall from 93.56% to ~93.7%

---

## Stage 2: full_test.rs Utility Functions (~100 lines)

**Target:** Cover pure utility functions that don't shell out.

### 2a. `rasterize_svg_to_png()` (~30 lines)
- Create test SVG input, verify PNG output dimensions and format
- Test error path (invalid SVG)

### 2b. `get_git_info()` (~15 lines)
- Run against the actual repo — returns git branch/commit info
- Test formatting output

### 2c. `set_low_priority()` (~10 lines)
- Call on Windows — just sets process priority, safe to test
- Verify it doesn't panic

### 2d. `print_help()` (~40 lines)
- Call and verify output (capture stdout or just ensure it doesn't panic)

### 2e. `check_d2_cli()` / `check_llvm_cov_available()` (~15 lines)
- Call against actual system — returns bool based on tool availability
- These are simple "can we find this tool?" checks

**Expected improvement:** ~100 lines → overall from ~93.7% to ~94.5%

---

## Stage 3: full_test.rs External Tool Wrappers (~150 lines)

**Target:** Cover functions that shell out to cargo/tools.

### 3a. `run_fmt_check()` (~30 lines)
- Actually runs `cargo fmt --check` — safe to call in tests
- Test the result parsing and QualityResult construction

### 3b. `run_clippy()` (~40 lines)
- Actually runs `cargo clippy` — safe to call
- Test result parsing

### 3c. `build_cargo_test_command()` edge cases
- Already partially tested; add more variant coverage

### 3d. `build_mdmdview_binary()` (~25 lines)
- Runs `cargo build --release` — expensive but testable
- May need to be gated with a slow-test flag

### 3e. `run_cargo_test()` — additional paths (~30 lines)
- Test with actual cargo test invocation (can use --no-run for speed)

**Expected improvement:** ~150 lines → overall from ~94.5% to ~95.6%

---

## Stage 4: full_test.rs Visual Test Logic (~200 lines)

**Target:** Cover visual test case runners using fixture data.

### 4a. `run_d2_visual_case()` (~100 lines)
- Create minimal D2 test fixtures (SVG + reference PNG)
- Test the comparison logic, diff image generation, pass/fail paths
- Test missing reference file path
- Test dimension mismatch path

### 4b. `run_d2_visual_tests()` orchestration (~50 lines)
- Test with empty case list
- Test with mock case directory

### 4c. `run_mermaid_case()` subset (~50 lines)
- The function requires the mdmdview binary — harder to test
- Focus on the comparison/diff portion rather than screenshot capture

**Expected improvement:** ~200 lines → overall from ~95.6% to ~97.1%

---

## Stage 5: full_test.rs Report Generation + main() (~150 lines)

**Target:** Cover remaining HTML report paths and entry point logic.

### 5a. `generate_html_report()` — uncovered branches (~80 lines)
- Add test variants: D2 visual results with failures, partial skip combinations
- Test the CSS dark mode section
- Test mermaid build failure rendering

### 5b. `main()` function — argument parsing paths (~50 lines)
- Test the options → execution flow
- May need to mark pure orchestration code as `coverage(off)`

### 5c. Remaining edge cases (~20 lines)
- `run_coverage()` parsing paths
- `generate_d2_references()` edge cases

**Expected improvement:** ~150 lines → overall from ~97.1% to ~98.2%

---

## Summary

| Stage | Focus | Lines | Cumulative Coverage |
|-------|-------|-------|-------------------|
| Baseline | — | 846 missed | 93.56% |
| 1 | Production gaps | ~15 | ~93.7% |
| 2 | full_test utilities | ~100 | ~94.5% |
| 3 | full_test tool wrappers | ~150 | ~95.6% |
| 4 | full_test visual tests | ~200 | ~97.1% |
| 5 | full_test reports + main | ~150 | ~98.2% |

**Commit strategy:** Commit after each stage, re-measure coverage to track progress.

---

## Notes

- `shell_integration.rs` (6 missed lines) is platform-conditional — NOT fixable on Windows
- Some `markdown_renderer.rs` gaps require complex UI state that may not be worth the test complexity
- `full_test.rs` functions that shell out to external tools are testable but slow — consider `#[ignore]` for CI
- If 98% proves unreachable due to genuinely untestable code, the appropriate tool is `#[cfg_attr(coverage_nightly, coverage(off))]` on specific functions with a comment explaining why
